apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: quay-operator-group
  namespace: quay-enterprise
spec:
  # Restringe el operador para que solo observe este namespace
  targetNamespaces:
    - quay-enterprise
